module.exports.register = function(Handlebars, options) {
	Handlebars.registerHelper('makerow', function(context, options) {
		// for (i = 0; i < context.length; i++) {
		// console.log(projects[i] + ", " + projects[i += 1]);
		return str;
	});
};
